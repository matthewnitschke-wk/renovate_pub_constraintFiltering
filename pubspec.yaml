name: constraint_filtering_demo

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  # Latest Dart-2-compatible: 2.4.2 (sdk >=2.19.0 <4.0.0)
  # Renovate bumps to:        2.7.0 (sdk ^3.3.0) — BREAKS
  args: ^2.3.1
